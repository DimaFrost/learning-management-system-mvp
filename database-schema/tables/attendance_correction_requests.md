<!-- Generated by npm run db:schema:sync. Do not edit by hand. -->

# attendance_correction_requests


Column count: 17

| Column | Type | Required | Nullable | Default | Description |
| --- | --- | --- | --- | --- | --- |
| id | integer:bigint | yes | no |  | Note:<br>This is a Primary Key.<pk/> |
| student_id | string:uuid | yes | no |  | Note:<br>This is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/> |
| course_id | integer:bigint | no | no |  | Note:<br>This is a Foreign Key to `courses.id`.<fk table='courses' column='id'/> |
| gate | string:text | yes | no |  |  |
| record_date | string:date | yes | no |  |  |
| title | string:text | yes | no |  |  |
| class_id | integer:bigint | no | no |  | Note:<br>This is a Foreign Key to `classes.id`.<fk table='classes' column='id'/> |
| well_week_start | string:date | no | no |  |  |
| ministry_session_id | integer:bigint | no | no |  | Note:<br>This is a Foreign Key to `ministry_service_sessions.id`.<fk table='ministry_service_sessions' column='id'/> |
| current_status | string:text | no | no |  |  |
| requested_status | string:text | yes | no |  |  |
| reason | string:text | yes | no |  |  |
| status | string:text | yes | no | pending |  |
| requested_at | string:timestamp with time zone | yes | no | now() |  |
| resolved_at | string:timestamp with time zone | no | no |  |  |
| resolved_by | string:uuid | no | no |  | Note:<br>This is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/> |
| resolution_note | string:text | no | no |  |  |

## Raw Definition

```json
{
  "required": [
    "id",
    "student_id",
    "gate",
    "record_date",
    "title",
    "requested_status",
    "reason",
    "status",
    "requested_at"
  ],
  "properties": {
    "id": {
      "description": "Note:\nThis is a Primary Key.<pk/>",
      "format": "bigint",
      "type": "integer"
    },
    "student_id": {
      "description": "Note:\nThis is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/>",
      "format": "uuid",
      "type": "string"
    },
    "course_id": {
      "description": "Note:\nThis is a Foreign Key to `courses.id`.<fk table='courses' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "gate": {
      "format": "text",
      "type": "string"
    },
    "record_date": {
      "format": "date",
      "type": "string"
    },
    "title": {
      "format": "text",
      "type": "string"
    },
    "class_id": {
      "description": "Note:\nThis is a Foreign Key to `classes.id`.<fk table='classes' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "well_week_start": {
      "format": "date",
      "type": "string"
    },
    "ministry_session_id": {
      "description": "Note:\nThis is a Foreign Key to `ministry_service_sessions.id`.<fk table='ministry_service_sessions' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "current_status": {
      "format": "text",
      "type": "string"
    },
    "requested_status": {
      "format": "text",
      "type": "string"
    },
    "reason": {
      "format": "text",
      "type": "string"
    },
    "status": {
      "default": "pending",
      "format": "text",
      "type": "string"
    },
    "requested_at": {
      "default": "now()",
      "format": "timestamp with time zone",
      "type": "string"
    },
    "resolved_at": {
      "format": "timestamp with time zone",
      "type": "string"
    },
    "resolved_by": {
      "description": "Note:\nThis is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/>",
      "format": "uuid",
      "type": "string"
    },
    "resolution_note": {
      "format": "text",
      "type": "string"
    }
  },
  "type": "object"
}
```
