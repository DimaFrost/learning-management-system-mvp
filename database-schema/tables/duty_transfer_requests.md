<!-- Generated by npm run db:schema:sync. Do not edit by hand. -->

# duty_transfer_requests


Column count: 11

| Column | Type | Required | Nullable | Default | Description |
| --- | --- | --- | --- | --- | --- |
| id | integer:bigint | yes | no |  | Note:<br>This is a Primary Key.<pk/> |
| duty_schedule_id | integer:bigint | yes | no |  | Note:<br>This is a Foreign Key to `duty_schedule.id`.<fk table='duty_schedule' column='id'/> |
| from_student_id | string:uuid | yes | no |  | Note:<br>This is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/> |
| to_student_id | string:uuid | yes | no |  | Note:<br>This is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/> |
| course_id | integer:bigint | yes | no |  | Note:<br>This is a Foreign Key to `courses.id`.<fk table='courses' column='id'/> |
| week_start | string:date | yes | no |  |  |
| reason | string:text | no | no |  |  |
| status | string:text | yes | no | pending |  |
| requested_at | string:timestamp with time zone | yes | no | now() |  |
| resolved_at | string:timestamp with time zone | no | no |  |  |
| resolved_by | string:uuid | no | no |  | Note:<br>This is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/> |

## Raw Definition

```json
{
  "required": [
    "id",
    "duty_schedule_id",
    "from_student_id",
    "to_student_id",
    "course_id",
    "week_start",
    "status",
    "requested_at"
  ],
  "properties": {
    "id": {
      "description": "Note:\nThis is a Primary Key.<pk/>",
      "format": "bigint",
      "type": "integer"
    },
    "duty_schedule_id": {
      "description": "Note:\nThis is a Foreign Key to `duty_schedule.id`.<fk table='duty_schedule' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "from_student_id": {
      "description": "Note:\nThis is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/>",
      "format": "uuid",
      "type": "string"
    },
    "to_student_id": {
      "description": "Note:\nThis is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/>",
      "format": "uuid",
      "type": "string"
    },
    "course_id": {
      "description": "Note:\nThis is a Foreign Key to `courses.id`.<fk table='courses' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "week_start": {
      "format": "date",
      "type": "string"
    },
    "reason": {
      "format": "text",
      "type": "string"
    },
    "status": {
      "default": "pending",
      "format": "text",
      "type": "string"
    },
    "requested_at": {
      "default": "now()",
      "format": "timestamp with time zone",
      "type": "string"
    },
    "resolved_at": {
      "format": "timestamp with time zone",
      "type": "string"
    },
    "resolved_by": {
      "description": "Note:\nThis is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/>",
      "format": "uuid",
      "type": "string"
    }
  },
  "type": "object"
}
```
