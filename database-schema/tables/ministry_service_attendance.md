<!-- Generated by npm run db:schema:sync. Do not edit by hand. -->

# ministry_service_attendance


Column count: 6

| Column | Type | Required | Nullable | Default | Description |
| --- | --- | --- | --- | --- | --- |
| id | integer:bigint | yes | no |  | Note:<br>This is a Primary Key.<pk/> |
| session_id | integer:bigint | yes | no |  | Note:<br>This is a Foreign Key to `ministry_service_sessions.id`.<fk table='ministry_service_sessions' column='id'/> |
| student_id | string:uuid | yes | no |  | Note:<br>This is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/> |
| status | string:text | yes | no | absent |  |
| marked_by | string:uuid | yes | no |  | Note:<br>This is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/> |
| marked_at | string:timestamp with time zone | yes | no | now() |  |

## Raw Definition

```json
{
  "required": [
    "id",
    "session_id",
    "student_id",
    "status",
    "marked_by",
    "marked_at"
  ],
  "properties": {
    "id": {
      "description": "Note:\nThis is a Primary Key.<pk/>",
      "format": "bigint",
      "type": "integer"
    },
    "session_id": {
      "description": "Note:\nThis is a Foreign Key to `ministry_service_sessions.id`.<fk table='ministry_service_sessions' column='id'/>",
      "format": "bigint",
      "type": "integer"
    },
    "student_id": {
      "description": "Note:\nThis is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/>",
      "format": "uuid",
      "type": "string"
    },
    "status": {
      "default": "absent",
      "format": "text",
      "type": "string"
    },
    "marked_by": {
      "description": "Note:\nThis is a Foreign Key to `profiles.id`.<fk table='profiles' column='id'/>",
      "format": "uuid",
      "type": "string"
    },
    "marked_at": {
      "default": "now()",
      "format": "timestamp with time zone",
      "type": "string"
    }
  },
  "type": "object"
}
```
